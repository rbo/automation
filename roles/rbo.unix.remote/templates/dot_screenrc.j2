startup_message off

term vt100

# utf8 on
# defutf8 on

#hardstatus on
#hardstatus alwayslastline "%{wk} chatt |  %c | %d.%m.%Y Load: %l | %w"
#hardstatus string "[%c] %{r}Hostname:%{d} %H | %{r}Screen:%{d} %t | %{r}Load:%{d} %l"

# Color table:
# 0 Black             .    leave color unchanged
# 1 Red               b    blue
# 2 Green             c    cyan
# 3 Brown / yellow    d    default color
# 4 Blue              g    green           b    bold
# 5 Purple            k    blacK           B    blinking
# 6 Cyan              m    magenta         d    dim
# 7 White             r    red             r    reverse
# 8 unused/illegal    w    white           s    standout
# 9 transparent       y    yellow          u    underline
#

caption always "%{  kw} {{ 'ratfink' if inventory_hostname == 'h2463879' else '%H' }} | %n %t | %-w%{.wr}%n %t%{  kw}%+w%{k}"

vbell_msg " *bell* "

windowlist string "(%3n) %{..r}[%{..R}%t%{..r}]%{..r}%=%f"

defscrollback     10000

autodetach on

escape ^Aa